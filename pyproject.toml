[project]
name = "vault-mcp"
version = "0.1.0"
description = "Obsidian vault MCP server + Deep Agents orchestration"
requires-python = ">=3.12"

[tool.uv]
package = false

[tool.uv.workspace]
members = ["shared", "obsidian_mcp", "agents", "api"]

[dependency-groups]
dev = ["ruff>=0.15", "pytest>=8", "pytest-asyncio>=0.24"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "W", "F", "I", "N", "UP", "B", "C4", "SIM", "ARG", "PIE", "T20", "PT", "RET", "PERF"]
# ARG001: tools receive a FastMCP-injected `ctx` they don't always read.
ignore = ["B008", "ARG001"]

[tool.ruff.lint.per-file-ignores]
# The demo script prints to the terminal by design (it's recorded as a GIF).
"docs/*" = ["T201"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
addopts = "-v"
